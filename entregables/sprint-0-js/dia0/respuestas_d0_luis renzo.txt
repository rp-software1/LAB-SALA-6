FEEDBACK:SeccionA: Durante el arranque de la actividad tuvimos varias complicaciones técnicas con el 
entorno local. Lo primero que tuvimos que hacer fue preparar las herramientas base: descargamos e instalamos Visual Studio Code 
para tener un editor adecuado y luego descargamos e instalamos Git para Windows desde su sitio oficial. Sin embargo, 
al abrir la terminal integrada en VS Code e intentar ejecutar el comando git init, el sistema nos arrojó el error git : El término 'git' 
no se reconoce.... Analizando el problema, nos dimos cuenta de que PowerShell no había actualizado las variables de entorno (PATH) 
y VS Code no detectaba la instalación de Git de forma automática. Para solucionarlo, tuvimos que reiniciar por completo el editor 
y configurar la terminal. Además, al momento de realizar el primer commit, Git detuvo la ejecución porque faltaba definir la identidad 
del autor en esta máquina. Tuvimos que ingresar manualmente los comandos git config --global user.email "luisxdghklkhv9@gmail.com" y git 
config --global user.name "luis890je" para asociar nuestros commits a nuestra cuenta. Superadas estas trabas de software, logramos avanzar 
con la dinámica Driver/Navigator. Repasamos la diferencia entre manejar archivos en Google Drive (donde se pierde el historial al sobrescribir)
 vs. Git (donde cada commit guarda una instantánea con trazabilidad total). Entendimos también la importancia del área de preparación 
 (staging area) mediante git add antes de confirmar los cambios. Finalmente, creamos la estructura requerida con la carpeta restaurante-js/, 
 redactamos el README.md respetando los saltos de línea, inicializamos el repositorio con git init, capturamos el archivo con git add README.md,
  ejecutamos git commit -m "Dia0-A: init repositorio restaurante-js" y verificamos el hash generado mediante git log --oneline. Cumplimos 
  los criterios del Bloque A y dejamos registrada la checklist.